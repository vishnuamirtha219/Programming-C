#include <stdio.h>
int main(){
    int num;
    printf("Enter the number: ");
    scanf("%d",&num);
    
    int count = 1;
    
    while(count <= 10)
    {
        int prod = num * count;
        printf("%d \n",prod);
        count = count+1;
    }return 0;
}
